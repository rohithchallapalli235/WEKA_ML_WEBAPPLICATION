name: CI / Build & Docker

on:
  push:
    branches: [ main, master ]
  pull_request:
    branches: [ main, master ]

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Python
        uses: actions/setup-python@v4
        with:
          python-version: '3.11'

      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          pip install -r requirements.txt

      - name: Quick syntax check
        run: |
          python -m py_compile app.py
          python -m py_compile weka_engine.py

      - name: Smoke import test
        run: |
          python -c "import weka_engine; print('weka_engine OK')"

  docker-build:
    needs: build
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Set up QEMU
        uses: docker/setup-qemu-action@v3

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3

      - name: Login to GitHub Container Registry
        if: secrets.GITHUB_TOKEN != ''
        uses: docker/login-action@v2
        with:
          registry: ghcr.io
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - name: Build and push image (to GHCR)
        uses: docker/build-push-action@v4
        with:
          context: .
          push: ${{ secrets.GITHUB_TOKEN != '' }}
          tags: ghcr.io/${{ github.repository }}:latest

# Optional deployment hints (uncomment & configure with your host's secrets)
#  deploy:
#    needs: docker-build
#    runs-on: ubuntu-latest
#    steps:
#      - name: Deploy to Render
#        uses: render-examples/deploy@v1
#        with:
#          apiKey: ${{ secrets.RENDER_API_KEY }}
#          serviceId: ${{ secrets.RENDER_SERVICE_ID }}
#
# Or add a Fly.io / Railway deploy step here and set secrets in your repo settings.
